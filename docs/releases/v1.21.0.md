# v1.21.0 — Phase O.1: Obsidian 完整语法补全

> **代号**: `obsidian-full` · 2026-09-04

## ✨ 新增

### 1. `%%comment%%` 行内注释

Obsidian 原生语法,整段在渲染时去掉,不显示。

例:
```markdown
这里显示 %%这是注释,不渲染%% 这里也显示
```

### 2. `mermaid` 代码块视觉标记

```markdown
```mermaid
flowchart TD
  A --> B
```
```

渲染时:
- 代码块背景:橙色渐变
- 左侧 3px 橙色边
- 右上角 "🧜 mermaid 流程图" 标签

(实际不渲染图,需第三方库;标记让用户知道这是 mermaid)

## 🔧 改动

| 文件 | 改动 |
|---|---|
| `src/services/renderer.ts` | + `obsidian_comment` inline rule |
| `src/styles.css` | + `.obsidian-comment` + `code.language-mermaid` 样式 |

## ✅ 验收

- [x] %% 注释静默去除
- [x] mermaid 视觉标记
- [x] tsc 通过
